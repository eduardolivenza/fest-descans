import * as React from "react";
import { AppLayout } from "layout";
import { ProductEditContainer } from "pods/product-edit";

export const ProductEditPage = () => (
  <AppLayout>   
    <ProductEditContainer/>
  </AppLayout>
);
